# Mission configuration
# Scenario 3: Asymmetric room with polygon boundary and solid interior obstacles

# Mapping boundary polygon (x,y in cm): right portion of room
boundary_polygon = (75,0),(200,0),(200,200),(75,200)

# Height range (cm): full room height
min_height_cm = 0
max_height_cm = 100

# Drone starting position (cm): left corridor, mid-height, clear of all obstacles
start_x_cm      = 100
start_y_cm      = 20
start_height_cm = 50

# Output resolution (decimal places after dot)
output_resolution_xy = 0
output_resolution_h  = 0

# Grid cell size for output map (cm); must match the input map resolution
map_resolution_cm = 5
