# Mission configuration
# Scenario 4: Two sources of -1 and -2 in the output map.
#
# SOURCE OF -2 (NotReachable / outside boundary):
#   The boundary polygon is inset 5 cm from the outer walls.
#   The four outer room walls (x=0, x=200, y=0, y=200) fall outside
#   the polygon and are therefore beyond the mapping boundary.
#   The drone detects them with lidar but MapBuilder discards the hits.
#   After post-processing, these cells appear as -2 in map_output.txt.
#
# SOURCE OF -1 (NotMapped / unreachable):
#   A completely sealed hollow box occupies x=80-120, y=80-120.
#   Its interior (x=85-115, y=85-115) is physically walled off with no
#   opening — lidar beams cannot penetrate the walls, and the drone has
#   no path inside.  Those cells remain NotMapped and appear as -1.

# Mapping boundary polygon (x,y in cm): inset 5 cm from outer walls
boundary_polygon = (5,5),(195,5),(195,195),(5,195)

# Height range (cm)
min_height_cm = 0
max_height_cm = 100

# Drone starting position (cm): lower-left area, well clear of the box
start_x_cm      = 50
start_y_cm      = 50
start_height_cm = 50

# Output resolution (decimal places after dot)
output_resolution_xy = 0
output_resolution_h  = 0

# Grid cell size for output map (cm); must match the input map resolution
map_resolution_cm = 5
